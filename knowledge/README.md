# Local Knowledge Base

Add `.md`, `.txt`, `.markdown`, or `.rst` files in this directory to make them searchable by the `rag_search` tool.

Use this folder for:

- Saved research notes
- Domain background
- Project-specific context
- Source excerpts you want the agent to reuse

The agent searches this local knowledge base before using web search when a topic may already have stored context.
